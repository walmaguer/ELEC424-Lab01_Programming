#define MAX_NUM 100

/* 
 * Sorts the integers in one array and outputs the sorted 
 * integers in another array.
 *
 * Inputs:
 *   input_array - Array containing integers to be sorted
 *   output_array - Array to place sorted integers in
 *   num - Number of elements in input array
 * Returns: Nothing
 */

void merge_sort(int input_array[], int output_array[], int num);
